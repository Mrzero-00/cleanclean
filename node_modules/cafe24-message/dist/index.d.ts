import { MessageConfig, MessageForm, MessageRepeatConfig, MessageType } from "./interface";
export default class Cafe24Message {
    private config;
    constructor(config: MessageConfig);
    send(form: MessageForm, forceMessageType?: MessageType): Promise<number>;
}
export { Cafe24Message, MessageConfig, MessageForm, MessageRepeatConfig, MessageType };
